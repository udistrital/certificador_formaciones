const fetchListarCohortesFormacion = (idFormacion, idTipoFormacion) => {
  //se hace el fetch, la respuesta se envia a:
  listarCohorterFormacion(
    "PARAMETRO LISTADO DE COHORTES FILTRADAS EN LA PETICION"
  );
};

export default fetchListarCohortesFormacion;
